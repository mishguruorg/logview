{
  "name": "@mishguru/logviewapi",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "publish": "npm run build && lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.13.1",
    "prettier": "^1.16.4",
    "typescript": "^3.3.3333"
  }
}
